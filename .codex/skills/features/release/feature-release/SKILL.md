---
name: feature-release
description: Enable and operate the Release feature (release checklists + changelog conventions + releasectl) for consistent versioning.
category: features/release
ssot_path: .ai/skills/features/release/feature-release
---

# feature-release (entry)

Canonical source: `.ai/skills/features/release/feature-release/`

Open `.ai/skills/features/release/feature-release/SKILL.md` and any supporting files referenced there (for example `reference.md`, `examples.md`, `scripts/`, `templates/`).

> **Note**: The frontmatter above is identical to the canonical source except for `ssot_path` and `category` which are added for navigation. After opening the source file, skip re-reading the description to avoid redundant token usage.
